apiVersion: v1
kind: Service
metadata:
  name: todo-react-service
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing
spec:
  type: LoadBalancer
  selector:
    app: todo-react
  ports:
    - port: 80
      targetPort: 80