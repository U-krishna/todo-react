apiVersion: v1
kind: Service
metadata:
  name: todo-react-service
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing
spec:
  selector:
    app: todo-react
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80