version: 0.2

phases:
  install:
    commands:
      - echo "Node: $(node -v)"
      - echo "NPM: $(npm -v)"
      - npm install

  build:
    commands:
      - npm run build

artifacts:
  files:
    - '**/*'
  base-directory: dist
cache:
  paths:
    - node_modules/**/* 